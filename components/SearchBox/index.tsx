
const SearchBox:React.FC = () => {
    return (
        <section>

        </section>
    )
}

export default SearchBox

export const SearchItem:React.FC = () => {
    return (
        <div>

        </div>
    )
}